{% extends 'base.html.twig' %}

{% block title %}Edit Preinscripcion{% endblock %}

{% block body %}
    <h1>Edit Preinscripcion</h1>

    {{ include('preinscripcion/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('preinscripcion_index') }}">back to list</a>

    {{ include('preinscripcion/_delete_form.html.twig') }}
{% endblock %}
